contour: sandbox

logging:
  app: DEBUG

database:
  url: jdbc:postgresql://89.108.103.170:5432
  username: fsrar_user
  password: Qwerty1234

# ... другие настройки
postgres:
  host: "localhost"
  port: 5432
  database: "test_db"
  user: "test_user"
  password: "test_password"
  table_name: "Test_table"

kafka:
  bootstrap_servers: "gitlab-ci.ru:9092"
  consumer_group_id: "frap-llm-helper-group"
  input_topic: "frap-llm-helper-in"
  output_topic: "frap-llm-helper-out"

file_storage:
  output_folder: "/app/data/output"
  file_prefix: "result_"
  cleanup_after_send: false  # В песочнице оставляем файлы для отладки